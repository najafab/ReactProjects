import React from "react";

const Props = ({ Name, University, Class }) => {
  return (
    <div>
      <p>{Name}</p>
      <p>{University}</p>
      <p>{Class}</p>
    </div>
  );
};

export default Props;
